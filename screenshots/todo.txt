Top Improvements (High Impact)

Add request cancellation for fast typing and screen changes.
Why: prevents stale responses and unnecessary API load.
Where: api.ts, backend.ts, app/(tabs)/search.tsx/search.tsx)
How: use AbortController and cancel in-flight fetch before sending new one.
Add rate-limit aware retry/backoff for 429.
Why: avoids noisy repeated failures and improves UX.
Where: api.ts, backend.ts
How: on 429, read Retry-After if present and retry once with short backoff.
Reduce Home re-fetch aggressiveness.
Why: lowers duplicate calls and avoids hitting throttle.
Where: app/(tabs)/index.tsx/index.tsx)
How: fetch on mount plus manual pull-to-refresh, not every focus event.
Prevent placeholder poster pollution.
Why: better visual quality and trust.
Where: app/(tabs)/search.tsx/search.tsx), MovieCard.tsx
How: filter items with missing poster_path or render a designed fallback card.
Normalize API response shape consistently.
Why: simplifies frontend null checks.
Where: MoviesController.php, AnalyticsController.php
How: always return data array or data object and include message/error fields consistently.

Performance Suggestions

Introduce a query/cache library (TanStack Query) for dedupe, caching, retry, and stale-while-revalidate behavior.
Tune FlatList rendering for movie grids:
initialNumToRender
maxToRenderPerBatch
windowSize
Add pagination for discover/search instead of single large payloads.
Use image placeholders and progressive loading to reduce perceived latency.
Cache trending in backend for short intervals (for example 30 to 60 seconds) to reduce DB hits.


Functionality Suggestions

Build Save tab with backend persistence:
Endpoints: add/remove/list watchlist
UI: saved cards + empty state + quick remove

Build Profile tab:
Basic user identity
Usage stats (search count, top genres)

Settings (theme, language, notifications)

Add filters on Home/Search:
Genre
Year
Rating
Sort by popularity or release date
Add trailer support on details screen with YouTube deep links.
Add Recently Viewed section driven by local storage.


New Feature Ideas

For You recommendations (content-based using recent searches and opens).
Trending this week and Trending by genre.
Smart search suggestions while typing.
Offline mode for saved movies and recent details.
Share movie feature with deep links to details screen.
Suggested Roadmap (Practical Order)

Stability sprint:
AbortController
429 backoff
Home fetch strategy cleanup
UX sprint:
Poster fallback polish
Skeleton loaders
Pull-to-refresh
Feature sprint:
Save tab backend + UI
Profile tab real data
Filters and recommendations
If you want, I can start immediately with the stability sprint and implement the first three items end-to-end in one pass.


